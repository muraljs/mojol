/* eslint-env mocha */
const { model, graphqlize } = require('../')

describe('graphqlize', () => {
  xit('generates a GraphQL schema from models', () => {
    console.log(graphqlize)
  })
})

describe('model', () => {
  xit('generates an object that can add middleware', () => {
    console.log(model)
  })
})
